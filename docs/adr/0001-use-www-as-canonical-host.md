# Use www as the canonical host

The portfolio can be served from either the apex domain or its `www` host, but competing public addresses would divide durable indexing and sharing signals. Use `https://www.josephtabalonjr.com` as the canonical site, permanently redirect the apex host to it, and align canonical metadata, the sitemap, social metadata, and structured data with that address.
